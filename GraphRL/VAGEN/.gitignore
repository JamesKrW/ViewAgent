# Generated artifacts live in the repository by default, but are never committed.
/build/
/data/
/runs/
/ckpt/
/outputs/
/transcripts/
/logs/
*.log
*.log.gz

# Vendored environment assets that are downloads, not source. spatial_gym/room_data is
# ~1 GB; symlink it in rather than committing it.
vagen/envs/spatial_gym/room_data/

__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# wandb writes run directories into the cwd.
wandb/
