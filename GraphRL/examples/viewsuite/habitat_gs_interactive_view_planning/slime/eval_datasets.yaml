eval:
  defaults:
    temperature: 1.0
    n_samples_per_eval_prompt: 1
    metadata_key: metadata
    input_key: prompt
    label_key: label

  datasets:
    - name: ${oc.env:VAGEN_EVAL_NAME}
      path: ${oc.env:VAGEN_EVAL_DATA}
